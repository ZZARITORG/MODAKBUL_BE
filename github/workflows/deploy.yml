# # .github/workflows/deploy.yml 예시
# name: Deploy to EC2

# on:
#   push:
#     branches: [ "main" ]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3

#       - name: Build Docker Image
#         run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/app-name .

#       - name: Docker Login
#         uses: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Push to Docker Hub
#         run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/app-name

#       - name: Deploy to EC2
#         uses: appleboy/ssh-action@master
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ${{ secrets.EC2_USER }}
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             docker pull ${{ secrets.DOCKERHUB_USERNAME }}/app-name
#             docker stop app-container || true
#             docker rm app-container || true
#             docker run -d --name app-container -p 3000:3000 ${{ secrets.DOCKERHUB_USERNAME }}/app-name
